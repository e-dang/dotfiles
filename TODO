MacOS Defaults
    change time in status bar to show date
    change default Finder window size to be larger
    change sidebar icon size to large
    change Finder window to start on documents and show harddrive
    change to dark mode by default
    change to hide dock automatically
    adjust type repeat speed
    change scroll directions to not natural
    show battery percentage
    ensure .DS_store is ignored globally
    dont show open apps next to downloads and trash can

.path
    source paths so it is not sourced before conda

brew
    add cmake