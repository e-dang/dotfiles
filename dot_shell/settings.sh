# this file acts as the "index.js" for the shell files
# in that you only need to source this one in order to get
# everything else. It is important to only source this file
# since the shell files must be sourced in the correct order
# as it is done here

source ~/.shell/aliases.sh
source ~/.shell/functions.sh
source ~/.shell/path.sh
source ~/.shell/plugins.sh
source ~/.shell/env.sh