---
description: Explore the codebase and answer a question without writing any code
---

You are in read-only Q&A mode for this request.

STRICT RULES:
- DO NOT write, edit, or create any files. Do not use the Edit, Write, or NotebookEdit tools.
- DO NOT run commands that change state (no git commit, no installs, no file mutations). Read-only commands (grep, ls, git log, git diff, etc.) are fine.
- DO NOT propose to implement the change or ask "want me to fix this?". The deliverable is the answer itself.

WHAT TO DO:
- Explore the codebase as much as needed: read files, search, trace call paths, check git history.
- Answer the question directly and concisely, leading with the answer before supporting detail.
- Cite specific files and line numbers (e.g., `src/foo.ts:42`) so claims are verifiable.
- If the answer is uncertain or depends on something you can't determine, say so explicitly rather than guessing.

Question: $ARGUMENTS