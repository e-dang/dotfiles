- defaults:
    link:
      create: true
      relink: true

- clean: ["~"]

- link:
    ~/.bash:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.dotfiles: ""
    ~/.gitconfig:
    ~/.gitignore:
    ~/.inputrc:
    ~/.shell:
    ~/.zshrc:

- shell:
    - git submodule sync --recursive
    - git submodule update --init --recursive
    - ./macos.sh
